Subject: Reset your <%= it.brand %> password

<% if (it.displayName) { %>Hi <%= it.displayName %>,<% } else { %>Hi,<% } %>

Someone asked to reset the password for your account. Choose a new one using
the link below.

<%= it.url %>

This link expires in <%= it.expiresIn %> and can only be used once.

Didn't ask for this? You can ignore this email — your password has not changed,
and the link above will expire on its own.

--
Sent by <%= it.brand %>. This is an automated message — replies to it are not read.
