Subject: You have been invited to join <%= it.organizationName %>

Hi,

<%= it.intro %>
<% if (it.roleLine) { %><%= it.roleLine %>
<% } %>
Use the link below to accept.

<%= it.url %>

If you do not have an account yet, the link will help you create one first —
<%= it.joinLine %>

This invitation expires in <%= it.expiresIn %> and can only be used once.

Not expecting this? You can ignore this email. Nothing has been created in your
name, and the link above will expire on its own.

--
Sent by <%= it.brand %>. This is an automated message — replies to it are not read.
